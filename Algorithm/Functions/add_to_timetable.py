def add_to_timetable(task_type):
    if task_type:
        # add task to timetable.json with timing = timing
        # and the block_type permanent
        pass
    else:
        # algorithm
        # add algorithm return
        pass